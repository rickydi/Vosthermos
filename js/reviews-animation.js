// Animation de défilement vertical désactivée car gérée par CSS
document.addEventListener('DOMContentLoaded', function() {
    console.log('Animation de carrousel vertical CSS activée');
    // Aucun code JavaScript nécessaire pour l'animation,
    // car elle est maintenant entièrement réalisée en CSS
    // avec la propriété animation et @keyframes scrollVert
});
